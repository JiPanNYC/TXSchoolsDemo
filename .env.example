# Optional local server port. Platforms such as Render/Railway provide PORT.
PORT=4174

# Optional API bind address. Use 127.0.0.1 behind nginx on a single EC2 host.
HOST=0.0.0.0

# Optional Vite dev proxy target when the API runs on a non-default port.
API_PROXY_TARGET=http://localhost:4174
